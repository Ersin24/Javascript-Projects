//Celvin
const kelvin = 0;
//Celcius
const celcius = kelvin - 273;
//Newton
let newton = celcius * (33/100);
newton = Math.floor(newton);
//Output newton scale
console.log(`The newton scale ${newton}`);